import { Routes } from "@angular/router";
import { LoginComponent } from "./components/login/login.component"; // Certifique-se de ajustar o caminho conforme necessário

export const routes: Routes = [{ path: "login", component: LoginComponent }];
